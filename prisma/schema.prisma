generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:adr%403412@localhost:3306/disnibra_cafe"
}

model Produto {
  id        Int      @id @default(autoincrement())
  nome      String
  preco     String   // Usando String para facilitar com a sua máscara de R$
  foto      String?  @db.LongText
  emoji     String?
  createdAt DateTime @default(now())
}